# Snapkite filter: Adult Content

This filter recognizes adult keywords in tweets.

## How to use

Rename `example.config.json` to `config.json` and replace `"foo", "bar", "baz"` with the list of your keywords.